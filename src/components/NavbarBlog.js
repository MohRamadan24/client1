import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'

const NavbarBlog = () => {

  return (
    <header className="flex w-full h-[100px] fixed flex-wrap bg-slate-transparen">
    </header>
  )
}

export default NavbarBlog